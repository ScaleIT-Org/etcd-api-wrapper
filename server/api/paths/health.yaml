x-swagger-router-controller: health
get:
  description: Returns 'HealthStatus' to the caller
  operationId: getHealthStatus
  responses:
    200:
      description: successful operation
      schema:
        $ref: "../definitions/HealthStatus.yaml"
    default:
      description: Error
      schema:
        $ref: "../definitions/Error.yaml"