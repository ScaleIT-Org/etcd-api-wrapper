Error:
  type: object
  required:
    - error
  properties:
    error:
      type: object
      required:
        - message
      properties:
        message:
          type: string

HealthStatus:
  type: object
  required:
    - health
  properties:
    health:
      type: string
      example: true