version: '3.1'
services:
  app-registry-mongodb:
    image: ${DOCKER_REGISTRY_ADDRESS}/app-registry-mongodb:v1.0.3
    build:
      context: ./../
      dockerfile: ./docker/PlatformEssentials/app-registry-mongodb/Dockerfile
  app-registration-sidecar-mongodb:
    image: ${DOCKER_REGISTRY_ADDRESS}/app-registration-sidecar-mongodb:v1.0.1
    build:
      context: ./../
      dockerfile: ./docker/PlatformSidecars/app-registration-sidecar-mongodb/Dockerfile